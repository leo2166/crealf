{
  "name": "crealf-app",
  "version": "1.0.0",
  "scripts": {
    "build": "flutter build web --release"
  },
  "dependencies": {
    "flutter_sdk": ">=3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}